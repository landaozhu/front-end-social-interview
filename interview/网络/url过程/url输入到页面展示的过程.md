# URL 输入后发生了什么

以下内容整理自本仓库历史讨论稿（原文笔误「dnc」已改为「DNS」）。



### 1. DNS查询
### 2. 建立 TCP 连接。
### 3. 发送http请求
### 4. 服务器处理请求并返回html报文
查库、读写文件、拼接 HTTP 响应。
### 4. 浏览器接收到首页 HTML。
### 5. 浏览器解析并渲染页面 
- 解析 HTML，形成 DOM Tree。
- 解析 CSS，形成 CSS Tree。
- 加载 JS 文件并执行 JS。

## 说明

实际面试中可再补充：TLS 握手（HTTPS）、HTTP/2 多路复用、解析与渲染主路径、合成层与绘制等，按岗位深度展开即可。
