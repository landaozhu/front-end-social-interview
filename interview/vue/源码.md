1 Compiler
template
↓
parse
↓
AST
↓
optimize
↓
codegen
↓
render函数

2 初始化
new Vue
↓
_init
↓
initState
↓
created
↓
$mount
↓
mountComponent
↓
new Watcher(updateComponent)

3 响应式
Observer -> Dep -> Watcher

4 虚拟DOM
render -> VNode

5 Diff
patch -> patchVnode -> updateChildren

6 更新机制
setter -> notify -> watcher -> nextTick -> patch