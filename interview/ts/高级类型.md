Recort<K,T> → 正确拼写 **Record**。

定义：把一堆 key 是 K 的东西，全部映射成 T。

```ts
const labels: Record<'admin' | 'user', string> = {
  admin: '管理员',
  user: '普通用户',
}
```

一面还要会：`Partial`（全可选）、`Pick`（挑字段）、`Omit`（删字段，底层 `Pick` + `Exclude`）。

完整表、手写映射、喜马拉雅向追问和文章链接：

→ [大厂TS高级类型.md](./大厂TS高级类型.md)
