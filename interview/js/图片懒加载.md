1. 原生（最简单）

<img loading="lazy" src="real.jpg" alt="">
浏览器自动处理，兼容性已足够好。

2. IntersectionObserver（面试最常问）
```javascript
<img data-src="real.jpg" src="placeholder.gif">
const observer = new IntersectionObserver((entries) => {
  entries.forEach(entry => {
    if (entry.isIntersecting) {
      const img = entry.target;
      img.src = img.dataset.src;
      observer.unobserve(img); // 加载后停止观察
    }
  });
});

document.querySelectorAll('img[data-src]').forEach(img => observer.observe(img));
```
优点：不绑 scroll，性能比 scroll + getBoundingClientRect 好。

3. scroll 监听（老方案） 滚动时算元素是否在视口内，命中则换 src。需要节流，否则滚动卡顿。

4. 工程化 / 框架

vue-lazyload、react-lazyload 等插件
Webpack 动态 import() 做组件懒加载（和图片懒加载是同一类思想）
5. 体验优化

先显示小图/模糊占位，大图加载完再替换（渐进式）
列表页配合虚拟列表，只渲染可见 DOM
